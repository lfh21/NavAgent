# 使用方法

## 1. 页面模式

当前网页端只保留两个模式，界面尽量简洁：

### Debug 模式

用途：

- 你手动上传一张图片
- 系统输出一份场景分析
- 适合调 prompt、看模型返回质量、排查图片输入问题

页面操作：

1. 选择 provider
2. 选择任务类型
3. 输入用户文字
4. 上传图片
5. 点击“开始调试分析”

返回内容：

- provider
- model
- summary
- riskLevel
- confidence
- guidance
- hazards
- tts

同时，后端会把图片和结果自动保存到 `output/debug/`。

### Formal 模式

用途：

- 实时接入浏览器摄像头
- 搭配实时文字输入
- 按固定时间间隔持续输出描述或寻路指令

页面操作：

1. 选择 provider
2. 选择任务类型
3. 输入实时文字指令
4. 点击“打开摄像头”
5. 点击“开始实时分析”
6. 点击“停止”

这个模式的“实时”是前端周期性截帧，再发送给后端，不是音视频流式传输。

## 2. Provider 说明

当前支持这些 provider：

- `mock`
- `openai`
- `qwen`
- `gemini`
- `kimi`
- `zhipu`

建议开发时先用 `mock`，接口稳定后再换真实 provider。

## 3. 接口说明

### `POST /api/debug/analyze`

用于 Debug 模式。

支持两种输入：

1. `multipart/form-data`
   - `image`
   - `provider`
   - `task`
   - `text`
2. `application/json`
   - `imageBase64`
   - `mimeType`
   - `provider`
   - `task`
   - `text`

### `POST /api/formal/analyze`

用于 Formal 模式。

JSON 输入示例：

```json
{
  "provider": "mock",
  "task": "navigation_guidance",
  "text": "持续告诉我前方障碍，并在需要时给我转向提示。",
  "frameBase64": "......",
  "mimeType": "image/jpeg",
  "sessionId": "live-demo-session"
}
```

### `POST /api/tts/payload`

输入：

```json
{
  "result": {
    "summary": "前方通道基本可通行。",
    "guidance": ["保持缓慢直行。"],
    "hazards": [],
    "riskLevel": "low"
  }
}
```

输出：

```json
{
  "ok": true,
  "ttsPayload": {
    "text": "前方通道基本可通行。保持缓慢直行。",
    "language": "zh-CN",
    "priority": "normal",
    "voiceHints": {
      "tone": "calm",
      "pace": "steady"
    }
  }
}
```

## 4. 推荐演示流程

如果你要做课程 Demo，建议这样展示：

1. 打开网页
2. 用 Debug 模式上传一张图片，展示单图分析
3. 切到 Formal 模式，打开摄像头
4. 输入“持续告诉我前方障碍，并在危险时给我避障建议”
5. 启动实时分析
6. 展示页面持续刷新 summary 和 guidance

这样能够直接覆盖截图里的三类核心模块：

- 前端交互
- 后端响应处理
- 大模型 API 接口

## 5. 当前限制

- 还没有做 WebSocket 实时流
- 还没有做连续会话记忆
- 还没有接真实 TTS 合成
- 正式模式当前是“轮询截帧”，不是“视频流编码上传”
