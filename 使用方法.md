# 使用方法

## 1. 项目说明

这是一个网页端盲人视觉辅助系统 Demo，当前提供两个主要模式：

- `图片导航模式`：上传单张图片，查看模型是否能正确理解场景、风险和用户问题。
- `视频导航模式`：调用浏览器摄像头，前端截取当前视频帧并发送给后端分析。
  - 在 `navigation_guidance` 任务下持续输出寻路指导。
  - 在 `scene_description` 或 `general_assistance` 任务下默认执行单次分析，更适合“我现在看到什么”“这是什么颜色”这类提问。

正式模式的实时链路是：

1. 浏览器打开本机摄像头。
2. 前端把当前视频帧压缩成 JPEG。
3. 前端把图片帧、文字指令和上一轮状态发送给后端。
4. 后端调用视觉大模型，返回结构化结果。
5. 后端生成 `ttsPayload`，并给出 `shouldSpeak`、`eventType`、`nextIntervalMs`。
6. 浏览器展示结果，并在需要时自动语音播报。

当前不是 WebSocket 视频流，也不是完整音视频流式传输；实现方式是“浏览器截帧 + HTTP 请求”。

## 2. 环境准备

### 2.1 Python 环境

建议使用：

- `Python 3.11+`

安装依赖：

```bash
pip install -r requirements.txt
```

### 2.2 模型 API 配置

项目运行时会先加载根目录下的 `.env.example`，再加载 `.env`。`.env` 中的值会覆盖默认配置，因此密钥建议只写在 `.env`。

首次使用可以复制模板：

```bash
cp .env.example .env
```

PowerShell：

```powershell
Copy-Item .env.example .env
```

当前代码支持这些 provider：

- `openai`
- `ernie`
- `qwen`
- `gemini`
- `kimi`
- `zhipu`
- `mock`，主要用于后端接口调试

`.env` 示例：

```env
APP_HOST=127.0.0.1
APP_PORT=8000
APP_DEBUG=true
DEFAULT_PROVIDER=qwen
FORMAL_INTERVAL_MS=1500
OUTPUT_DIR=output

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-2.5-flash

ERNIE_API_KEY=
ERNIE_BASE_URL=https://llmapi.paratera.com
ERNIE_MODEL=ERNIE-4.5-Turbo-VL-32K

QWEN_API_KEY=
QWEN_BASE_URL=
QWEN_MODEL=Qwen3.6-Plus

KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn/v1
KIMI_MODEL=kimi-k2.6

ZHIPU_API_KEY=
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4
ZHIPU_MODEL=glm-4v-flash
```

说明：

- `DEFAULT_PROVIDER`：网页默认优先使用的模型。
- `FORMAL_INTERVAL_MS`：视频导航模式默认基础间隔，单位毫秒。
- `OUTPUT_DIR`：调试图片、JSON 结果和正式模式日志的保存目录。
- `OPENAI`、`ERNIE`、`QWEN`、`KIMI`、`ZHIPU` 当前按 OpenAI 兼容接口调用，通常需要 `API_KEY` 和 `BASE_URL`。
- `Gemini` 使用单独 REST 接口，至少需要 `GEMINI_API_KEY`。

## 3. 启动项目

在项目根目录执行：

```bash
python main.py
```

也可以使用 CLI：

```bash
python cli.py runserver
```

指定端口：

```bash
python cli.py runserver --host 127.0.0.1 --port 8000
```

查看当前配置：

```bash
python cli.py show-config
```

启动成功后，终端会打印地址，默认是：

```text
http://127.0.0.1:8000/
```

如果要使用摄像头，建议使用 `http://127.0.0.1:8000/` 或 `http://localhost:8000/` 打开。浏览器通常只允许 HTTPS 或 localhost 场景调用摄像头。

## 4. 页面功能

### 4.1 图片导航模式

用途：

- 上传一张图片。
- 调试模型对场景、障碍、风险和问题的理解。
- 检查 provider、prompt 和图片质量。

操作步骤：

1. 点击 `图片导航模式`。
2. 选择 provider。
3. 选择任务类型。
4. 输入你的问题，或点击 `语音输入` 通过麦克风录入。
5. 上传图片。
6. 点击 `开始调试分析`。

返回内容包括：

- `provider`
- `model`
- `summary`
- `riskLevel`
- `confidence`
- `guidance`
- `hazards`
- `ttsPayload`
- `savedArtifacts`

调试模式会自动把图片和结果保存到：

- `output/debug/`

### 4.2 视频导航模式

用途：

- 调用浏览器摄像头。
- 结合用户输入的问题或指令分析当前画面。
- 在寻路任务下持续给出短指令。
- 在场景描述或综合辅助任务下完成单次视觉问答。

操作步骤：

1. 保持在 `视频导航模式`。
2. 选择 provider。
3. 选择任务类型。
4. 输入实时文字指令，或点击 `语音输入`。
5. 点击 `打开摄像头`。
6. 点击 `开始实时分析` 或 `分析一次`。
7. 如需结束，点击 `停止`。

正式模式当前特点：

- 前端会将视频帧缩放并压缩为 JPEG，再发送给后端。
- 后端返回结构化结果，不只是一段普通文本。
- `navigation_guidance` 会持续轮询，适合连续盲行引导。
- `scene_description` 和 `general_assistance` 默认只分析一次，不会连续自动播报。
- 当风险较高、风险变化、指令变化或用户强制刷新时，更容易触发语音播报。
- 后端会返回建议的 `nextIntervalMs`，前端据此动态调整下一轮分析节奏。
- 如果前后两帧变化很小且风险较低，前端可能跳过一次请求，减少重复分析。

正式模式日志会保存在：

- `output/formal/<sessionId>.jsonl`

## 5. 任务类型说明

### `scene_description`

用于描述当前画面和主要风险，不输出导航建议。适合问：

- “前方环境是什么样？”
- “这张图里有什么危险？”
- “我现在看到什么？”

### `navigation_guidance`

用于持续寻路指导，会优先输出可执行动作。适合问：

- “持续提醒我前方障碍。”
- “我应该往哪里走？”
- “危险时告诉我停下或转向。”

### `general_assistance`

用于综合视觉问答，只回答用户问题，不输出导航风险。适合问：

- “这个杯子是什么颜色？”
- “图片里有几个人？”
- “门牌上写了什么？”

## 6. 语音输入与语音播报

### 语音输入

页面中的 `语音输入` 按钮会调用浏览器语音识别接口：

- Chrome / Edge 通常支持。
- 如果浏览器不支持，页面会提示“当前浏览器不支持语音输入”。
- 需要授予麦克风权限。

### 语音播报

当前项目有两条语音链路：

- 网页端直接用浏览器 `speechSynthesis` 播报。
- 后端返回结构化 `ttsPayload`，供下游 Python TTS 或其他语音模块使用。

`ttsPayload` 示例：

```json
{
  "text": "前方基本可通行，左前方一米有障碍，请略向右。",
  "language": "zh-CN",
  "priority": "high",
  "voiceHints": {
    "tone": "calm",
    "pace": "steady"
  }
}
```

## 7. 视频导航模式快捷键

为了便于盲人用户操作，视频导航模式支持以下快捷键：

- `Space`：开始 / 暂停 / 继续实时分析。
- `S`：播报当前最新结果。
- `X`：停止当前语音播报。
- `-`：基础间隔减少 `100ms`，分析更快。
- `+`：基础间隔增加 `100ms`，分析更慢。

说明：

- 快捷键只在视频导航模式中生效。
- 当焦点在输入框或文本框中时，除空格控制外，不会抢占正常输入。
- 间隔越小，请求越频繁，模型调用次数也会更多。

## 8. 摄像头权限说明

视频导航模式通过浏览器原生接口 `navigator.mediaDevices.getUserMedia(...)` 调用摄像头。

如果第一次打开摄像头失败，请检查：

- 浏览器地址栏是否拒绝了摄像头权限。
- Windows 或 macOS 系统隐私设置是否允许浏览器使用摄像头。
- 是否有其他软件正在占用摄像头。
- 是否通过 `127.0.0.1`、`localhost` 或 HTTPS 打开页面。

常见报错：

- `Permission denied`：浏览器或系统权限没有打开。
- `当前浏览器不支持摄像头接口`：当前浏览器环境不支持 `getUserMedia`。
- `摄像头已打开，但视频流尚未就绪`：已申请到设备，但视频流尚未稳定。

## 9. 返回结果说明

无论是图片导航还是视频导航，后端都会把模型输出整理成统一结构：

```json
{
  "summary": "前方两米内基本可通行，左侧有椅子。",
  "guidance": [
    "保持缓慢直行。",
    "经过左侧障碍时略向右。"
  ],
  "hazards": [
    {
      "type": "obstacle",
      "severity": "medium",
      "description": "左前方约一米处有静态障碍物。"
    }
  ],
  "riskLevel": "medium",
  "confidence": 0.88,
  "task": "navigation_guidance",
  "sourceText": "持续提醒我前方障碍。"
}
```

字段含义：

- `summary`：一句话概括当前环境、答案或主要风险。
- `guidance`：建议执行的动作；在 `scene_description` 和 `general_assistance` 中可能为空。
- `hazards`：识别到的风险点；在 `general_assistance` 中固定为空。
- `riskLevel`：总体风险级别。
- `confidence`：模型对结果的把握程度。
- `task`：本次任务类型。
- `sourceText`：用户输入文本。

风险等级：

- `low`
- `medium`
- `high`
- `unknown`

视频导航模式额外字段：

- `shouldSpeak`：这一轮是否建议播报。
- `eventType`：`stable`、`change`、`warning` 或 `danger`。
- `nextIntervalMs`：后端建议的下一轮分析间隔。

## 10. 接口说明

详细接口见 `docs/api-contract.md`。常用接口包括：

- `GET /api/health`
- `GET /api/providers`
- `POST /api/debug/analyze`
- `POST /api/formal/analyze`
- `POST /api/tts/payload`

### `POST /api/debug/analyze`

支持 `multipart/form-data`：

- `image`
- `provider`
- `task`
- `text`

也支持 JSON：

```json
{
  "provider": "qwen",
  "task": "scene_description",
  "text": "请描述前方环境",
  "imageBase64": "......",
  "mimeType": "image/jpeg",
  "fileName": "debug.jpg"
}
```

### `POST /api/formal/analyze`

输入示例：

```json
{
  "provider": "qwen",
  "task": "navigation_guidance",
  "text": "持续告诉我前方障碍，并在需要时给我转向提示。",
  "frameBase64": "......",
  "mimeType": "image/jpeg",
  "sessionId": "live-demo-session",
  "requestedIntervalMs": 1500,
  "frameMeta": {
    "timestamp": "2026-05-31T12:00:00.000Z",
    "width": 640,
    "height": 360
  },
  "clientState": {
    "lastSummary": "前方基本可通行。",
    "guidance": ["保持缓慢直行。"],
    "lastRiskLevel": "low",
    "lastSpokenText": "前方基本可通行。保持缓慢直行。",
    "forceDetailed": false
  }
}
```

## 11. 推荐演示流程

如果用于课程展示，可以按这个流程：

1. 启动项目并打开网页。
2. 在图片导航模式上传一张图片，展示单图分析。
3. 切回视频导航模式。
4. 输入“持续告诉我前方障碍，并在危险时给我避障建议”。
5. 打开摄像头并开始实时分析。
6. 展示页面如何持续刷新 `summary`、`guidance`、`hazards`。
7. 演示语音输入、自动播报、手动播报和快捷键。
8. 打开 `output/debug/` 或 `output/formal/` 展示本地归档结果。

这样能直接覆盖：

- 前端交互
- 后端响应处理
- 大模型 API 接口
- 实时语音播报
- 盲操作辅助
- 本地日志归档

## 12. 当前限制

当前版本仍然有这些限制：

- 还没有做 WebSocket 实时流。
- 还没有做真正的视频流推理。
- 还没有接入独立的真实 TTS 合成服务。
- 语音输入依赖浏览器支持和麦克风权限。
- 视频导航依赖浏览器摄像头权限。
- 长时间运行时只维护轻量状态，没有复杂的会话记忆与多轮上下文压缩。

## 13. 常见问题

### 13.1 页面里模型显示“未配置”

通常是：

- 没填对应的 `API_KEY`。
- `BASE_URL` 没填或写错。
- `.env` 没放在项目根目录。
- 使用了前端未展示的 `mock` provider。

### 13.2 打开摄像头失败：`Permission denied`

请检查：

- 浏览器站点权限是否允许摄像头。
- 系统隐私设置是否允许该浏览器使用摄像头。
- 是否被其他软件占用。
- 是否通过 localhost、127.0.0.1 或 HTTPS 访问页面。

### 13.3 语音输入不可用

可能原因：

- 当前浏览器不支持 `SpeechRecognition` 或 `webkitSpeechRecognition`。
- 麦克风权限被拒绝。
- 浏览器语言或系统语音服务不可用。

建议优先使用 Chrome 或 Edge 测试。

### 13.4 为什么正式模式不是连续视频流

因为当前版本追求：

- 更简单的网页部署。
- 更稳定的跨浏览器兼容性。
- 更容易给上游 Python 模块和下游 TTS 模块预留接口。

所以目前采用“截帧 + HTTP 请求”的方式，而不是直接传输整段视频流。
