# 使用方法

## 1. 项目说明

这是一个网页端盲人辅助系统 Demo，当前提供两个主要模式：

- `Debug 模式`：上传单张图片，查看模型是否能正确理解场景。
- `Formal 模式`：调用浏览器摄像头，周期性截取视频帧，实时输出场景描述、风险提醒和寻路指导。

当前正式模式的实时链路是：

- 浏览器打开本机摄像头
- 前端把当前视频帧压缩成 JPEG
- 前端把图片帧和文字指令发送给后端
- 后端调用视觉大模型返回结构化结果
- 浏览器自动语音播报，或者把 `ttsPayload` 交给下游 TTS 模块

它不是 WebSocket 视频流，也不是完整音视频流式传输；当前是“浏览器截帧 + HTTP 请求”的实现。

## 2. 环境准备

### 2.1 Python 环境

建议使用：

- `Python 3.10+`

安装依赖：

```bash
pip install -r requirements.txt
```

### 2.2 模型 API 配置

项目运行时通过根目录下的 `.env` 配置模型，不需要改 Python 代码。

当前代码支持这些 provider：

- `openai`
- `ernie`
- `qwen`
- `gemini`
- `kimi`
- `zhipu`

建议你在项目根目录创建或编辑 `.env`，最少写成这样：

```env
APP_HOST=127.0.0.1
APP_PORT=8000
APP_DEBUG=true
DEFAULT_PROVIDER=qwen
FORMAL_INTERVAL_MS=1500
OUTPUT_DIR=output

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-2.5-flash

ERNIE_API_KEY=
ERNIE_BASE_URL=https://llmapi.paratera.com
ERNIE_MODEL=ERNIE-4.5-Turbo-VL-32K

QWEN_API_KEY=
QWEN_BASE_URL=
QWEN_MODEL=Qwen3.6-Plus

KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn/v1
KIMI_MODEL=kimi-k2.6

ZHIPU_API_KEY=
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4
ZHIPU_MODEL=glm-4v-flash
```

说明：

- `DEFAULT_PROVIDER`：网页默认优先使用的模型。
- `FORMAL_INTERVAL_MS`：正式模式默认基础间隔，单位毫秒。
- `QWEN`、`OPENAI`、`ERNIE`、`KIMI`、`ZHIPU` 当前按 OpenAI 兼容接口方式调用，所以通常都需要 `API_KEY` 和 `BASE_URL`。
- `Gemini` 使用单独 REST 接口，至少要有 `GEMINI_API_KEY`。

## 3. 启动项目

在项目根目录执行：

```bash
python main.py
```

启动成功后，终端会打印地址，默认是：

```text
http://127.0.0.1:8000/
```

然后：

1. 打开浏览器访问这个地址。
2. 浏览器会加载网页前端。
3. 你可以选择 `Debug` 或 `Formal` 模式。

说明：

- 当前页面默认进入 `Formal` 模式。
- 如果你要使用摄像头，建议用 `http://127.0.0.1:8000/` 或 `http://localhost:8000/` 打开，不要优先用局域网 IP。

## 4. 摄像头权限说明

正式模式通过浏览器原生接口 `navigator.mediaDevices.getUserMedia(...)` 调用电脑摄像头。

如果第一次打开摄像头失败，请重点检查：

- 浏览器地址栏是否拒绝了摄像头权限
- macOS `系统设置 -> 隐私与安全性 -> 摄像头` 是否给浏览器授权
- 是否有其他软件正在占用摄像头

常见报错：

- `Permission denied`：通常是浏览器或系统权限没有打开。
- `当前浏览器不支持摄像头接口`：浏览器环境不支持 `getUserMedia`。
- `摄像头已打开，但视频流尚未就绪`：浏览器已申请到设备，但视频流还没稳定。

## 5. 页面模式

### 5.1 Debug 模式

用途：

- 上传一张图片
- 查看模型对场景的理解是否正确
- 调试 prompt、模型配置和图片输入质量

操作步骤：

1. 切到 `图片导航模式`
2. 选择 provider
3. 选择任务类型
4. 输入你的问题
5. 上传图片
6. 点击 `开始调试分析`

返回内容包括：

- `provider`
- `model`
- `summary`
- `riskLevel`
- `confidence`
- `guidance`
- `hazards`
- `ttsPayload`

调试模式会自动把图片和结果保存到：

- `output/debug/`

### 5.2 Formal 模式

用途：

- 调用浏览器摄像头
- 配合用户输入的文字指令实时分析
- 连续输出场景描述、风险提醒和行动建议

操作步骤：

1. 保持在 `视频导航模式`
2. 选择 provider
3. 选择任务类型
4. 输入实时文字指令
5. 点击 `打开摄像头`
6. 点击 `开始实时分析`
7. 如需结束，点击 `停止`

正式模式当前的特点：

- 前端会压缩视频帧，再发送给后端
- 后端返回结构化结果，不只是一段普通文本
- 当风险较高或场景变化较大时，更容易触发语音播报
- 后端会返回建议的 `nextIntervalMs`，前端据此动态调整下一轮分析节奏
- 如果前后两帧变化很小，前端可能会跳过一次请求，减少重复分析

正式模式日志会保存在：

- `output/formal/`

## 6. Formal 模式的盲操作快捷键

为了便于盲人用户操作，正式模式支持以下快捷键：

- `Space`：开始 / 暂停 / 继续实时分析
- `S`：播报当前最新结果
- `X`：停止当前语音播报
- `-`：基础间隔减少 `100ms`，分析更快
- `+`：基础间隔增加 `100ms`，分析更慢

页面中也提供了对应按钮：

- `播报当前结果`
- `停止播报`
- `更快 -100ms`
- `更慢 +100ms`

说明：

- 如果当前还没有分析结果，按 `S` 不会播报有效内容。
- 调整基础间隔后，页面状态栏会提示当前间隔值。
- 间隔越小，请求越频繁，模型调用次数也会更多。

## 7. 返回结果说明

无论是 Debug 还是 Formal，后端都会把模型输出整理成统一结构，核心字段通常包括：

```json
{
  "summary": "前方两米内基本可通行，左侧有椅子。",
  "guidance": [
    "保持缓慢直行。",
    "经过左侧障碍时略向右。"
  ],
  "hazards": [
    {
      "type": "obstacle",
      "severity": "medium",
      "description": "左前方约一米处有静态障碍物。"
    }
  ],
  "riskLevel": "medium",
  "confidence": 0.88
}
```

字段含义：

- `summary`：一句话概括当前环境和主要风险
- `guidance`：建议执行的动作
- `hazards`：识别到的风险点
- `riskLevel`：总体风险级别
- `confidence`：模型对结果的把握程度

风险等级：

- `low`
- `medium`
- `high`
- `unknown`

## 8. TTS 说明

当前项目有两条语音链路：

- 网页端直接用浏览器 `speechSynthesis` 播报
- 后端返回结构化 `ttsPayload`，供下游 Python TTS 模块使用

`ttsPayload` 的典型结构：

```json
{
  "text": "前方基本可通行，左前方一米有障碍，请略向右。",
  "language": "zh-CN",
  "priority": "high",
  "voiceHints": {
    "tone": "calm",
    "pace": "steady"
  }
}
```

说明：

- `text`：最终播报文本
- `language`：语言
- `priority`：播报优先级
- `voiceHints`：给下游语音模块的语气和语速建议

## 9. 接口说明

### 9.1 `GET /api/health`

返回服务状态、默认 provider 和正式模式基础间隔。

### 9.2 `GET /api/providers`

返回当前前端可选择的 provider 列表，以及哪些 provider 已经正确配置。

### 9.3 `POST /api/debug/analyze`

用于 Debug 模式。

支持两种输入。

`multipart/form-data`：

- `image`
- `provider`
- `task`
- `text`

`application/json`：

```json
{
  "provider": "qwen",
  "task": "scene_description",
  "text": "请描述前方环境",
  "imageBase64": "......",
  "mimeType": "image/jpeg",
  "fileName": "debug.jpg"
}
```

### 9.4 `POST /api/formal/analyze`

用于 Formal 模式。

输入示例：

```json
{
  "provider": "qwen",
  "task": "navigation_guidance",
  "text": "持续告诉我前方障碍，并在需要时给我转向提示。",
  "frameBase64": "......",
  "mimeType": "image/jpeg",
  "sessionId": "live-demo-session",
  "requestedIntervalMs": 1500,
  "frameMeta": {
    "timestamp": "2026-05-30T12:00:00.000Z",
    "width": 640,
    "height": 360
  },
  "clientState": {
    "lastSummary": "前方基本可通行。",
    "guidance": ["保持缓慢直行。"],
    "lastRiskLevel": "low",
    "lastSpokenText": "前方基本可通行。保持缓慢直行。",
    "forceDetailed": false
  }
}
```

返回示例：

```json
{
  "ok": true,
  "mode": "formal",
  "provider": "qwen",
  "model": "Qwen3.6-Plus",
  "sessionId": "live-demo-session",
  "result": {
    "summary": "前方两米内基本可通行，左前方有障碍。",
    "guidance": ["保持缓慢直行。", "略微向右调整。"],
    "hazards": [
      {
        "type": "obstacle",
        "severity": "medium",
        "description": "左前方约一米处存在静态障碍物。"
      }
    ],
    "riskLevel": "medium",
    "confidence": 0.86
  },
  "ttsPayload": {
    "text": "前方基本可通行，左前方一米有障碍，请略向右。",
    "language": "zh-CN",
    "priority": "high",
    "voiceHints": {
      "tone": "calm",
      "pace": "steady"
    }
  },
  "shouldSpeak": true,
  "eventType": "warning",
  "nextIntervalMs": 1200
}
```

新增字段说明：

- `shouldSpeak`：这一轮是否建议播报
- `eventType`：当前变化类型，如 `stable`、`change`、`warning`、`danger`
- `nextIntervalMs`：后端建议的下一轮分析间隔

### 9.5 `POST /api/tts/payload`

如果你已经有结构化结果，也可以单独请求后端把结果整理成 TTS 载荷。

输入示例：

```json
{
  "result": {
    "summary": "前方通道基本可通行。",
    "guidance": ["保持缓慢直行。"],
    "hazards": [],
    "riskLevel": "low"
  }
}
```

## 10. 推荐演示流程

如果你要做课程展示，建议这样演示：

1. 启动项目并打开网页
2. 在 Debug 模式上传一张图片，展示单图分析
3. 切回 Formal 模式
4. 输入“持续告诉我前方障碍，并在危险时给我避障建议”
5. 打开摄像头并开始实时分析
6. 展示页面如何持续刷新 `summary`、`guidance`、`hazards`
7. 演示快捷键：
   - `Space` 暂停和继续
   - `S` 播报当前结果
   - `X` 停止播报
   - `- / +` 调整分析节奏

这样能直接覆盖：

- 前端交互
- 后端响应处理
- 大模型 API 接口
- 实时语音播报
- 盲操作辅助

## 11. 当前限制

当前版本仍然有这些限制：

- 还没有做 WebSocket 实时流
- 还没有做真正的视频流推理
- 还没有接入独立的真实 TTS 合成服务
- 正式模式依然依赖浏览器摄像头权限
- 长时间运行时还没有做更复杂的会话记忆与多轮上下文压缩

## 12. 常见问题

### 12.1 页面里模型显示“未配置”

通常是：

- 没填对应的 `API_KEY`
- `BASE_URL` 没填或写错
- `.env` 没放在项目根目录

### 12.2 打开摄像头失败：`Permission denied`

请检查：

- 浏览器站点权限是否允许摄像头
- 系统隐私设置是否允许该浏览器使用摄像头
- 是否被别的软件占用

### 12.3 为什么正式模式不是连续视频流

因为当前版本追求的是：

- 更简单的网页部署
- 更稳定的跨浏览器兼容性
- 更容易给上游 Python 模块和下游 TTS 模块预留接口

所以目前采用“截帧 + 请求”的方式，而不是直接传输整段视频流。
