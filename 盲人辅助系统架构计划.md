# 盲人辅助系统网页版架构计划

## 1. 架构目标

把项目改造成一个可以直接演示的极简网页系统，同时满足课程截图中的要求：

- 有前端交互
- 有后端大模型响应处理
- 有大模型 API 接口
- 最好再带一点额外功能

## 2. 模块划分

### 前端

位置：

- `frontend/index.html`
- `frontend/styles.css`
- `frontend/app.js`

职责：

- Debug 模式上传图片
- Formal 模式接入摄像头
- 采集实时文本
- 切换 provider
- 展示分析结果

### 后端

位置：

- `backend/api_interface.py`
- `backend/text_utils.py`
- `backend/file_tools.py`

职责：

- 接收图片或视频帧
- 组织 prompt
- 调用模型客户端
- 规范化模型 JSON 输出
- 生成 TTS 载荷
- 保存本地调试结果

### 模型接口层

位置：

- `backend/llm_client.py`
- `backend/config.py`

职责：

- 抽象 OpenAI / DeepSeek / Zhipu / Mock 四类 provider
- 统一把视觉输入包装成多模态请求
- 对外返回统一 JSON 结果

## 3. 双模式设计

### Debug 模式

输入：

- 单张图片
- 用户文字
- provider
- task

输出：

- summary
- guidance
- hazards
- riskLevel
- confidence
- ttsPayload

特点：

- 单次分析
- 可控、稳定
- 自动保存到本地 `output/debug/`

### Formal 模式

输入：

- 浏览器摄像头实时视频帧
- 用户文字
- provider
- task

输出：

- 场景描述
- 寻路指令
- TTS 载荷

特点：

- 前端定时截帧
- 后端按帧分析
- 准实时

## 4. 额外功能

课程截图中的“其他任何想实现的功能”，当前版本用下面两项覆盖：

1. 本地调试结果归档
2. TTS 载荷接口

## 5. 目录结构

```text
agent-demo-api/
├── backend/
│   ├── __init__.py
│   ├── api_interface.py
│   ├── config.py
│   ├── file_tools.py
│   ├── llm_client.py
│   └── text_utils.py
├── frontend/
│   ├── app.js
│   ├── index.html
│   └── styles.css
├── cli.py
├── file_utils.py
├── main.py
├── output/
├── README.md
└── requirements.txt
```

## 6. 接口层

- `GET /`
- `GET /api/health`
- `GET /api/providers`
- `POST /api/debug/analyze`
- `POST /api/formal/analyze`
- `POST /api/tts/payload`

## 7. 演示策略

最小演示建议：

1. 打开网页
2. Debug 模式上传单张图，展示结果
3. Formal 模式打开摄像头，展示实时截帧分析

这个演示已经明显比截图里的“本地文件智能体助手示例 Demo”更贴近真实用户场景。
