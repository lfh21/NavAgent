# 盲人辅助系统网页版架构计划

## 1. 架构目标

项目目标是实现一个可直接课堂演示的网页端盲人视觉辅助系统，覆盖课程要求中的四类能力：

- 前端交互：图片上传、摄像头接入、任务选择、语音播报和语音输入。
- 后端大模型响应处理：统一 prompt、结果规范化、风险等级整理、TTS 载荷生成。
- 大模型 API 接口：封装多个视觉大模型 provider。
- 额外功能：本地结果归档、正式模式自适应播报、盲操作快捷键和基础日志。

## 2. 总体链路

```text
浏览器页面
  ├─ 图片导航：上传图片 + 文本问题
  └─ 视频导航：摄像头画面截帧 + 文本问题
        ↓ HTTP
Flask 后端接口
        ↓
Prompt 构造与模型调用
        ↓
结构化结果规范化
        ↓
TTS 载荷 / 播报决策 / 日志归档
        ↓
浏览器展示与语音播报
```

正式模式目前采用“浏览器截帧 + HTTP 请求”，不是 WebSocket 视频流，也不是完整音视频流式传输。这样实现更简单，便于课程演示和跨浏览器调试。

## 3. 模块划分

### 前端

位置：

- `frontend/index.html`
- `frontend/styles.css`
- `frontend/app.js`

职责：

- 提供图片导航模式和视频导航模式。
- 加载 provider 列表并提示 provider 是否已配置。
- 在视频导航模式中打开摄像头、等待视频帧就绪、截取并压缩 JPEG。
- 维护正式模式会话状态，包括上一轮摘要、风险等级、播报文本和画面变化签名。
- 根据后端返回的 `shouldSpeak` 控制浏览器语音播报。
- 支持语音输入和盲操作快捷键。

### 后端接口层

位置：

- `backend/api_interface.py`

职责：

- 提供页面和静态资源路由。
- 提供健康检查、provider 列表、调试分析、正式分析和 TTS 载荷接口。
- 接收 multipart 图片或 JSON base64 图片帧。
- 调用模型客户端、规范化结果、保存输出文件。
- 为正式模式维护轻量 `sessionId -> state` 状态。

### 文本与反馈处理层

位置：

- `backend/text_utils.py`

职责：

- 构造系统 prompt 和用户 prompt。
- 为不同任务设置不同约束：
  - `scene_description`：只描述场景和风险，不给行走建议。
  - `navigation_guidance`：优先给可执行寻路指令。
  - `general_assistance`：直接回答用户视觉问题，不输出导航风险。
- 从模型输出中提取 JSON。
- 规范化字段、风险等级、置信度和 hazards。
- 生成适合语音播报的 `ttsPayload`。
- 根据上一轮状态生成 `eventType`、`shouldSpeak` 和 `nextIntervalMs`。

### 模型接口层

位置：

- `backend/llm_client.py`
- `backend/config.py`

职责：

- 加载 `.env.example` 和 `.env` 配置。
- 封装 OpenAI 兼容接口：`openai`、`ernie`、`qwen`、`kimi`、`zhipu`。
- 封装 Gemini REST API。
- 提供 `mock` 结果用于后端链路调试。
- 统一把图片 data URL 和文本问题包装成多模态请求。

### 文件工具层

位置：

- `backend/file_tools.py`
- `file_utils.py`

职责：

- 创建输出目录。
- 保存调试图片和 JSON。
- 追加正式模式 JSONL 日志。
- 处理 base64 图片、data URL、文件名安全化和项目路径。

## 4. 双模式设计

### 图片导航模式

输入：

- 单张图片
- 用户文字
- provider
- task

输出：

- `summary`
- `guidance`
- `hazards`
- `riskLevel`
- `confidence`
- `ttsPayload`
- `savedArtifacts`

特点：

- 单次分析，结果稳定。
- 可用于调试模型、prompt、图片质量和 provider 配置。
- 自动保存到 `output/debug/`。

### 视频导航模式

输入：

- 浏览器摄像头当前帧
- 用户文字
- provider
- task
- `sessionId`
- 前端上一轮状态 `clientState`

输出：

- 结构化视觉结果
- TTS 载荷
- 是否建议播报 `shouldSpeak`
- 事件类型 `eventType`
- 后端建议下一轮间隔 `nextIntervalMs`

特点：

- 前端定时截帧，最大宽度约 640px，JPEG 质量约 0.7。
- `navigation_guidance` 可持续轮询，适合连续盲行辅助。
- `scene_description` 和 `general_assistance` 默认分析一次后停止。
- 前端会在低风险且画面变化很小时跳过重复请求。
- 后端会在高风险、风险变化、指令变化或首次结果时更容易触发播报。
- 日志写入 `output/formal/<sessionId>.jsonl`。

## 5. 额外功能设计

当前版本用于覆盖“其他功能”的实现包括：

- 本地结果归档：调试模式保存图片和 JSON，正式模式保存 JSONL。
- TTS 载荷接口：后端统一生成适合下游语音模块的文本与语气建议。
- 浏览器语音播报：网页直接使用 `speechSynthesis`。
- 浏览器语音输入：支持 Chrome/Edge 的 `SpeechRecognition` 或 `webkitSpeechRecognition`。
- 盲操作快捷键：空格开始/暂停/继续，`S` 播报，`X` 停止，`- / +` 调整间隔。
- 正式模式自适应节奏：风险高时更快，稳定时放慢。

## 6. 接口层

- `GET /`
- `GET /assets/<filename>`
- `GET /api/health`
- `GET /api/providers`
- `POST /api/debug/analyze`
- `POST /api/formal/analyze`
- `POST /api/tts/payload`

接口字段见 `docs/api-contract.md`。

## 7. 目录结构

```text
NavAgent/
├── backend/
├── docs/
├── frontend/
├── output/
├── cli.py
├── file_utils.py
├── main.py
├── pyproject.toml
├── requirements.txt
├── README.md
├── 使用方法.md
└── 盲人辅助系统架构计划.md
```

## 8. 演示策略

推荐课堂演示流程：

1. 启动服务并打开网页。
2. 进入图片导航模式，上传测试图片，展示单图结构化分析。
3. 切到视频导航模式，打开摄像头。
4. 输入“持续提醒我前方障碍，危险时告诉我停下或转向”。
5. 开始实时分析，展示摘要、导航建议、风险提醒和语音播报。
6. 演示语音输入、手动播报和快捷键。

这个演示能同时体现多模态输入、后端大模型处理、实时辅助交互和无障碍操作思路。
